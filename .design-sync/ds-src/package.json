{
  "name": "generations-wellness-ds",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "React component library for the Generations Wellness v2 \"ATMOSPHERES\" design system. Each component emits the exact markup and class names used on generationswellness.net; all styling comes from the site's own tokens.css + main.css.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "node ./sync-css.mjs && tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "dependencies": {
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "react-dom": "^18.3.1",
    "typescript": "^7.0.2"
  }
}
